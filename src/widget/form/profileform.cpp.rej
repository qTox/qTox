--- src/widget/form/profileform.cpp
+++ src/widget/form/profileform.cpp
@@ -180,11 +180,11 @@ void ProfileForm::setToxId(const QString& id)
 
 void ProfileForm::onAvatarClicked()
 {
-    // nullptr - qfiledialog doesn't inherit CSS from `this`
-    QString filename = QFileDialog::getOpenFileName(nullptr,
-        tr("Choose a profile picture"),
-        QDir::homePath(),
-        Nexus::getSupportedImageFilter());
+    // 0 - qfiledialog doesn't inherit CSS from `this`
+    QString filename = QFileDialog::getOpenFileName(0,
+                                                tr("Choose a profile picture"),
+                                                QDir::homePath(),
+                                                Nexus::getSupportedImageFilter());
     if (filename.isEmpty())
         return;
     QFile file(filename);
